#!/usr/bin/env bash

# set -e

if [ "${#}" -eq 1 ] && [[ "${1}" != "-"* ]]
then
    RESULT=$("${JUMP_DIR}/jump_cli" "${@}")
    if [ ! -z "${RESULT}" ]
    then             
        cd "${RESULT}"
    fi
else
    "${JUMP_DIR}/jump_cli" "${@}"
fi